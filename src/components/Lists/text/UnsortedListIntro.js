const UnsortedListIntro = () => {
  return (
    <header className="comp_intro">
      <h1>Unsorted list</h1>
      <p>
        Here we repeat the same exercise as in sorted lists, but allowing the
        list items to be added to a random position in the list. This adds
        complexity when identifying the elements to remove.
      </p>
    </header>
  );
};
export default UnsortedListIntro;
